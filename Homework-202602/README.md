# R语言作业说明

本仓库用于提交本次 R 语言作业代码，主要包括两个任务：包的查找、安装与查看，以及自定义函数对随机向量进行阈值判断。

## 作业内容

### 任务1：查找、安装并查看 tidyverse 包
在 RStudio 中新建 R Script 文件，通过 CRAN 完成以下操作：
1. 查找 `tidyverse` 包  
2. 安装 `tidyverse` 包  
3. 加载并查看 `tidyverse` 包的信息  

### 任务2：自定义函数并进行阈值判断
编写一段 R 脚本，自定义一个函数，用于检查随机向量

```r
rnorm(n = 10, mean = 35, sd = 10)